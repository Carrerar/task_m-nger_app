{
  "name": "focus-board",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local productivity board: tasks, timers, recurring schedules, calendar and dashboard. Vanilla ES modules, no runtime dependencies.",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "test": "node --test",
    "serve": "node serve.mjs"
  }
}
